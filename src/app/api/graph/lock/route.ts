import { NextResponse } from "next/server";
import { z } from "zod";
import { createSupabaseServerClient } from "@/lib/supabase/server";
const bodySchema=z.object({selector:z.object({seed_id:z.string().uuid()}).strict(),idempotency_key:z.string().uuid()}).strict(); const graph=z.object({id:z.string().uuid(),agent_snapshot_id:z.string().uuid(),version:z.literal("phase3-graph-snapshot-v1"),graph_locked:z.literal(true),locked_at:z.string().datetime(),safety_level:z.enum(["safe","caution"]),error_code:z.null()}).strip();
function trace(){return `graph_lock_${crypto.randomUUID()}`;} function fail(status:number,error_code:string,trace_id:string){return NextResponse.json({ok:false,error_code,trace_id},{status});} function map(m:string):readonly[number,string]{if(m==="seed_not_found")return[404,m];if(["agent_snapshot_invalid","graph_snapshot_invalid","graph_locked","safety_downgraded","safety_blocked","idempotency_key_content_conflict"].includes(m))return[409,m];if(m==="evidence_required")return[400,m];return[500,"persistence_failed"];} export async function POST(request:Request){const trace_id=trace();try{const supabase=await createSupabaseServerClient();if(!supabase)return fail(500,"persistence_failed",trace_id);const {data:auth}=await supabase.auth.getUser();if(!auth.user?.id)return fail(401,"unauthenticated",trace_id);if(request.headers.get("content-type")?.split(";",1)[0]?.trim().toLowerCase()!=="application/json")return fail(400,"invalid_request",trace_id);const parsed=bodySchema.safeParse(await request.json().catch(()=>null));if(!parsed.success)return fail(400,"invalid_request",trace_id);const {data,error}=await supabase.rpc("lock_relation_graph_phase3",{p_seed_context_id:parsed.data.selector.seed_id,p_idempotency_key:parsed.data.idempotency_key});if(error){const[s,c]=map(error.message);return fail(s,c,trace_id);}if(!Array.isArray(data)||data.length!==1||!data[0]||typeof data[0]!=="object")return fail(500,"persistence_failed",trace_id);const row=data[0] as {idempotent?:unknown;graph?:unknown};const g=graph.safeParse(row.graph);if(typeof row.idempotent!=="boolean"||!g.success)return fail(500,"persistence_failed",trace_id);return NextResponse.json({ok:true,error_code:null,trace_id,idempotent:row.idempotent,graph:g.data},{status:row.idempotent?200:201});}catch{return fail(500,"persistence_failed",trace_id);}}
